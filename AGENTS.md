# 開発ガイドライン

## 最優先事項

コードの生成、修正、リファクタリング、レビューでは、他人がコードを理解し、変更し、デバッグするまでの時間を最短にする。短さ、技巧性、過剰な抽象化より、誤解されにくさ、実行経路の追いやすさ、変更容易性を優先する。

## 既存コードを変更する前の確認

- 関連クラス、呼び出し元、基底クラス、設定ファイル、ビルド設定、既存の命名規則を確認する。
- 存在を確認できていないメソッド、型、API、ファイル構成を推測で作らない。不明点は推測であることを明示する。
- 現在の処理の流れと依存関係を確認し、依頼されていない既存動作を変えない。
- 変更・追加・削除するファイルを明示する。宣言、定義、呼び出し、include、ビルド設定の整合性を確認する。
- 古い API や不要なラッパー、重複した状態管理による不整合がないか確認する。
- ビルドや実行をしていない場合は、確認済みであるかのように断言しない。

## 命名

- `data`、`value`、`result`、`tmp`、`manager`、`process` など、役割が分からない名前を安易に使わない。
- 変数、関数、クラスの名前から、対象、処理、結果を分かるようにする。名前で表せるならコメントで補わない。
- 時間、距離、角度、速度には、必要に応じて `Seconds`、`Milliseconds`、`Meters`、`Degrees` などの単位を含める。
- bool は `is`、`has`、`can`、`should`、`was` などで始め、肯定形にする。
- `Get` は軽量なアクセサだけに用いる。検索は `Find`、計算は `Calculate`、解決は `Resolve`、判定は `Can`、`Should`、`Is` を用いる。
- 一般的でない略語を避ける。

## 設計と制御フロー

- クラスと関数は一つの明確な責務またはタスクを担わせる。異なる理由で変更される処理を分離する。
- 同じ関数内の処理の抽象度をそろえ、汎用的な下位問題だけを分離する。
- 数行ごとの過剰な関数化や、相互依存する小クラスの乱造はしない。分割で実行経路が追いにくくなる場合は分割しない。
- 依存関係、所有権、状態の変更箇所、データの流れを明示し、グローバル状態と暗黙の副作用を減らす。
- ガード節と早期 return でネストを浅くし、正常系を直線的に読めるようにする。
- 複雑な条件式は意味のある説明変数または判定関数へ分ける。変化する値を比較式の左側、基準値を右側に置く。
- 否定の重なり、技巧的な短絡評価、一行への複数処理の詰め込み、明確な利点のない三項演算子、`do/while`、`goto`、複雑なマクロを避ける。

## 変数とインタフェース

- 変数は使用直前に定義し、スコープを最小限にする。変更しない値には `const` を付ける。
- 一つの変数を複数の意味に使い回さず、再代入を減らす。式の意味を明確にする説明変数は使うが、単なる別名は作らない。
- 制御フローのためだけのフラグは、早期 return、`break`、関数分割で不要にできないか検討する。
- 呼び出し側だけで意図が分かるインタフェースにする。引数を最小限にし、関連引数が増えたら設定構造体や値オブジェクトを検討する。
- 意味が分からない bool 引数を避け、必要なら enum や明確な型を使う。高コストな処理を軽量な Getter のように見せない。
- エラー時の挙動、戻り値、所有権を明確にする。

## コメント

- コードがしている処理を日本語で繰り返さない。
- コメントには、処理や設計が必要な理由、不自然に見える処理の理由、仕様上の制約や前提、精度・性能・安全性のトレードオフ、誤用されやすい境界条件、既知の問題や意図的な妥協を記す。
- 正確かつ簡潔に書き、「これ」「それ」など対象が曖昧な表現を避ける。不要なコメントを追加しない。

## C++

- RAII を優先し、生の `new` と `delete` を避ける。ポインタ、参照、スマートポインタを所有権に応じて使い分ける。
- 入力のみの引数には `const` 参照を検討する。不要なコピーを避けるが、可読性を犠牲にした過剰最適化はしない。
- 使用する機能に必要なヘッダを明示的に include する。実験的な拡張、非標準機能、危険なマクロは必要性を確認してから使う。
- 未定義動作、null、寿命、イテレータ無効化を考慮する。

## テストと回答

- テスト名から対象、条件、期待結果を分かるようにし、振る舞いごとに分ける。
- 最も単純な入力を使い、必要に応じて境界値、空入力、重複、null、不正値を扱う。失敗時は期待値と実際値が分かるようにする。
- テストしにくい場合は、依存関係や責務の設計を見直す。
- 「コード全体」または「そのまま貼り替えられる形」と依頼された場合は、省略記号を使わず、対象ファイルの完全なコードを正確なパスとともに提示する。部分修正と完全置換を混在させない。
- 既存の命名規則、インデント、波括弧、メンバ接頭辞、ファイル構成に一貫性があれば優先する。ただし明確な欠陥は無条件に踏襲せず、問題点を説明する。

半年後に実装を忘れた本人や初めて読む人が、コメントに頼りすぎず処理を追えるコードを目標にする。リーダブルコードの原則は機械的に適用せず、正しさ、既存設計との一貫性、性能、安全性、変更規模のバランスを取って判断する。
